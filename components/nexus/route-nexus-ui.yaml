---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: nexus-ui
  namespace: nexus
  annotations:
    openshift.io/host.generated: "true"
spec:
  port:
    targetPort: nexus-ui
  tls:
    termination: edge
  to:
    kind: Service
    name: nexus
    weight: 100
  wildcardPolicy: None

